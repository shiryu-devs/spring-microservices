spring:
  application:
    name: auth-service
  datasource:
    password: desarrollo
    url: jdbc:mysql://localhost:3306/bd_spring?zeroDateTimeBehavior=convertToNull
    username: axartel
  jpa:
    hibernate:
      ddl-auto: update
  cloud:
    config:
      enabled: true
      uri: http://localhost:8053
jwt:
  secret: secret
admin-paths:
  paths:
    -
      uri: "/users"
      method: "POST"
    -
      uri: "/users/(1)[1-9]+[0-9]*/cars/(1)"
      method: "POST"
    -
      uri: "/users/(1)[1-9]+[0-9]*/bikes/(1)"
      method: "POST"